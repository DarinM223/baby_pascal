let compile _program =
  (* let module Fresh = Normalize.Fresh () in *)
  (* let = Normalize.normalize Fresh.fresh expr in *)
  (* let program =
    { program with Ast.main = Normalize.normalize Fresh.fresh program.Ast.main }
  in *)
  failwith ""
